jobs:
 - name: daily
   targetName: dev 
   schedule: 0 12 * * * 
   timeout: 2h30m
   steps:
     - name: run models 
       command: dbt run src_nba_raw
     - name: test models
       command: dbt test src_nba_raw

